spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
  profiles:
     active: native

server:
  port: 9000

security:
  user: 
    password: YFzCAfocMInyJ5YaO805